#!/bin/bash
if [ -f '<%= @user_home_dir %>/.ssh/id_rsa.pub' ]; then
  key=$(cat <%= @user_home_dir %>/.ssh/id_rsa.pub | cut -d ' ' -f 2)
  echo "<%= scope['psick::bolt::bolt_user'] %>_user_key=${key}"
fi
